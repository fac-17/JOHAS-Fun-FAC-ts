const test = require("tape");
const buildScript = require("../model/database/build");
const queries = require("../model/queries/queries");

test("Tape is working", t => {
  t.equal(1, 1, "1 equals 1");
  t.end();
});
